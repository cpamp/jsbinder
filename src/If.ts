import { IfBinder } from "./IfBinder";
import { Element } from "./Element";

export namespace If {
    export function bindIfElements(scope: Object, ifAttribute: string, ifBinders: IfBinder, binders: Element[]) {
        
    }
}